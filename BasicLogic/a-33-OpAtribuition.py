"""
Operadores de atribuição
= += -= *= /= //= **= %=
"""
contador = 10

###

# contador = contador + 1 é o mesmo que contador + = 1 soma

# contador = contador - 1 é o mesmo que contador -= 1 subtração

# contador = contador * 1 é o mesmo que contador *= 1 multiplicação

# contador = contador / 1 é o mesmo que contador /= 1 divisão

# contador = contador // 1 é o mesmo que contador //= 1 divisão inteira

# contador = contador % 1 é o mesmo que contador %= 1 resto da divisão




